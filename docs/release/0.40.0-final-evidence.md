# TelePilot 0.40.x 最终版证据台账

> 当前 0.40.1 封口证据请看 `docs/release/0.40.1-final-evidence.md`。本文件保留为 0.40.0/0.40.x 历史基线，不作为当前最终版 Go / No-Go 结论来源。

状态：历史自动验证记录存在；当前 0.40.1 工作区仍需重新跑全量验证、远端部署、浏览器/PWA 验收和回滚演练

本文件最初为 `0.40.0` 最终版 Go / No-Go 证据台账骨架。封口补丁进入 `0.40.1` 后，本文件只能作为 `0.40.x` 台账继续维护；若最终发布版本不是 `0.40.0`，最终报告必须以当前版本为准，并可以复制/迁移为 `docs/release/<version>-final-evidence.md`。除本轮已经实际运行且对应当前 commit 的验证外，其他条目默认标记为“待验证”，不得把计划、推测、历史验证或子任务口头结论写成通过。

## 0.40.1 封口提示

- 当前版本文件已进入 `0.40.1`，最终证据、部署报告、远端版本和 CHANGELOG 必须按当前版本填写。
- 下方“本轮验证记录”如果早于当前工作区改动，只能作为历史基线；当前工作区合并 runtime、文档或前端改动后必须重新执行完整验证。
- 任何没有重新验证的控制项，即使历史记录显示“已完成”，也必须在最终报告里降级为 `可测` 或 `待验证`。

## 19.12 证据台账

| 区块 | 必填内容 | 当前状态 | 证据记录 | 剩余风险 |
| --- | --- | --- | --- | --- |
| 分支和版本 | 本地分支、远端分支、commit、四处版本号、CHANGELOG 版本段 | 部分已验证 | 本地分支 `codex/0.33-interaction-framework`；当前工作区版本已进入 `0.40.1`：`backend/app/__init__.py`、`backend/pyproject.toml`、`frontend/package.json`、`frontend/src/lib/version.ts`；`CHANGELOG.md` 已新增 `0.40.1` 中文段 | commit、push 后需回填远端 commit；需重新跑验证后才可把 0.40.1 标为已完成 |
| 变更范围 | 本次实际改动文件分组、公共契约变化、兼容层位置 | 部分已验证 | 当前 diff 覆盖 Trace/Event Bus 后端、日志 API/UI、插件 manifest、远程插件字段、示例插件、验证脚本、开发文档、版本与 CHANGELOG | 最终 commit 前需再读 `git status --short --branch -uall` |
| 五条链路 | 普通消息、管理员命令、callback、inline、payment 的 trace/span/action 证据 | 部分已验证 | 自动测试覆盖 `test_event_bus.py`、`test_event_trace.py`、`test_account_bot.py`、`test_worker_command.py`；`test_transfer_notice_event_bus_payment_subscription_without_legacy_rule` 覆盖无旧付款规则的 payment Event Bus 投递 | 远端真实 Telegram trace 和日志页展示仍待部署后验证 |
| blocker 关闭 | 第 19.11 节每个 blocker 的关闭方式、测试名和剩余风险 | 部分已验证 | 自动测试覆盖回滚开关、旧通道失败、native_raw/raw_event 边界、插件加载失败状态、action failed 记录、reason_code 字典和 Event Bus matcher | 线上回滚开关演练和 UI 实测仍待验证 |
| 自动验证 | 第 18.2 节每条命令、退出码、失败原因或替代证据 | 历史记录 | 见“本轮验证记录”；`alembic upgrade head` 在线模式因本机 PostgreSQL/Docker 不可用未能本地连接验证，但 `alembic heads` 和 `alembic upgrade head --sql` 通过，真实迁移需部署时验证 | 当前 0.40.1 证据以 `docs/release/0.40.1-final-evidence.md` 为准 |
| 人工验收 | 第 19.8 节每个场景的页面 URL、账号/插件、观察结果 | 待验证 | 待部署后在 `https://telebot.260505.xyz/` 验证 | 桌面、窄屏/PWA、日志页 trace 均待实测 |
| 文档审计 | 第 19.6 节 grep 结果分类：合法迁移、历史说明、测试覆盖、已修复 | 历史记录 | 本轮执行第 19.6 节 grep，旧机制 695 条、新契约 726 条、payload 主路径 22 条、发送通道 1947 条、Trace 覆盖 354 条；已按下方分类记录 | 当前 0.40.1 文档审计以新版台账为准 |
| 部署证据 | 远端 commit、版本号、迁移、健康检查、关键 trace、docker 日志 | 待验证 | 待部署阶段填写 | 本轮不做远端部署 |
| 回滚证据 | 备份路径、关闭开关演练结果、旧日志可用性 | 待验证 | 待部署/回滚演练填写 | 本轮不验证回滚开关 |

## 最终版状态控制板

本控制板对应 `docs/TELEGRAM-FULL-EVENT-BUS-TRACE-PLAN.md` 第 19.19 节。状态只允许使用：`未开始`、`半落地`、`可测`、`已完成`。没有证据的条目不得标为已完成。

| 控制项 | 当前状态 | 证据入口 | 剩余风险 |
| --- | --- | --- | --- |
| 数据层和迁移 | 可测 | `backend/alembic/versions/0031_event_trace.py`，`alembic heads` = `0031 (head)`，`alembic upgrade head --sql` 生成 873 行 SQL | 本机 PostgreSQL/Docker 不可用，在线 upgrade 待远端部署验证 |
| 五条事件链路 | 可测 | `pytest -q`、`test_event_bus.py`、`test_event_trace.py`、`test_account_bot.py`、`test_worker_command.py` | 真实 Telegram 触发链路和日志页展示待远端验收 |
| 插件契约 | 历史记录 | manifest 字段、Event Bus、MessageOps、Contract Guard、reason_code 均有后端测试、前端类型、文档和验证脚本覆盖 | 当前状态以 0.40.1 台账为准 |
| 官方和示例插件 | 历史记录 | 官方/内置 manifest 和 plugin.json 补齐 usage/event_subscriptions/capabilities；`validate-plugin-examples.py` 通过 | 当前状态以 0.40.1 台账为准 |
| 远程插件仓库 | 可测 | 远程插件 service/schema/frontend 类型保留新字段；验证脚本和测试覆盖私有仓库、`tree/<branch>`、一键更新字段 | 升级前风险变化预览需远端 UI 实测 |
| 日志中心 | 可测 | `frontend tsc` / `vite build` 通过；`Logs.tsx` 覆盖 reason_code、native_raw、inline、action 详情 | 桌面和窄屏/PWA 待部署后浏览器验收 |
| 交互中心 | 可测 | `BotTab.tsx` / `Interaction` 页面类型构建通过，展示订阅、能力和风险提示 | 规则列表、详情高度、窄屏体验待浏览器验收 |
| 插件 UI | 可测 | 插件中心和通用配置页构建通过，usage 缺失、配置容器、契约展示有代码覆盖 | 缺 usage 红色高级警告待浏览器确认 |
| 设置和回滚 | 可测 | 测试覆盖 `trace_enabled`、`event_bus_delivery_enabled`、`inline_updates_enabled`、native_raw 保留策略 | 远端/临时环境开关演练待补 |
| 开发文档 | 历史记录 | README、PLUGIN-API-REFERENCE、PLUGIN-REMOTE、PLUGIN-CHEATSHEET、PLUGIN-SAFETY 已更新并审计 | 当前状态以 0.40.1 台账为准 |
| 发布材料 | 可测 | 四处版本号 `0.40.1`，`CHANGELOG.md` 中文 `0.40.1` 段 | 当前工作区仍需重新验证、commit、push；最终部署报告必须按 0.40.1 填写 |
| 服务器部署 | 待验证 | 待填写备份、远端 commit、迁移、健康检查、关键 trace、docker logs | 不能只写“已部署” |

## 最终版防缩水条款记录

| 条款 | 当前状态 | 需要补充的证据 |
| --- | --- | --- |
| 19.20 端到端验收数据包 | 历史记录 | `examples/plugins/event_bus_demo/fixtures/*`；`validate-plugin-examples.py`；`test_event_bus.py` / `test_event_trace.py` / `test_account_bot.py` |
| 19.21 数据库、设置和迁移闭环 | 可测 | Alembic offline SQL、Trace 清理测试、native_raw 保留测试、回滚开关测试 | 在线迁移和开关演练待远端/临时环境 |
| 19.22 运行时稳态和性能边界 | 历史记录 | Trace best-effort fallback、订阅粗过滤、payload/native_raw 截断、rate_limited、failed action 测试 | 后续新增链路需继续补 reason_code |
| 19.23 前端实测脚本和验收入口 | 待验证 | 待远端浏览器验收 | 当前本机后端/DB 不可用，先不以本地页面冒充通过 |
| 19.24 文档最终同步包 | 历史记录 | README 与插件开发文档已按最终定位更新；grep 审计已分类 |
| 19.25 残余风险允许范围 | 部分已验证 | 用户安装插件 warning 已列入允许残余风险；Alembic 在线/浏览器/回滚待验证 | No-Go 类风险当前未由自动验证发现 |
| 19.26 最终版宣称边界 | 历史记录 | `CHANGELOG.md` 与插件开发指南均限定为框架落地，不宣称封装所有 Telegram 能力 |

## 文档审计记录

### 旧机制审计

命令：

```bash
rg -n "notice|bbot_notice|notice_bot|raw_event|平铺 payload|旧规则驱动|event\\.reply|event\\.respond|ctx\\.client\\.send_message" README.md docs backend frontend examples scripts
```

状态：已执行，未宣称最终通过。

| 分类 | 命中范围 | 处理状态 |
| --- | --- | --- |
| 合法迁移说明 | `README.md`、`docs/PLUGIN-API-REFERENCE.md`、`docs/PLUGIN-REMOTE.md`、`docs/PLUGIN-CHEATSHEET.md`、`docs/PLUGIN-SAFETY.md`、`docs/TELEGRAM-FULL-EVENT-BUS-TRACE-PLAN.md` 中的旧 `notice`、旧平铺 payload、旧 `raw_event`、旧 hook 说明 | 已改为迁移/废弃/兼容说明；不作为新插件主路径 |
| 合法历史说明 | `docs/INTERACTION-OPEN-EVENT-FRAMEWORK-PLAN.md`、`docs/INTERACTION-BOT-OPTIMIZATION.md`、`docs/SECURITY-OPS.md`、旧 hook 游戏骨架 | 保留历史上下文；需最终文档 Owner 判断是否归档 |
| 测试故意覆盖 | `backend/app/tests/test_*`、`backend/app/tests/test_plugin_security_regression.py`、验证脚本中的 `DEPRECATED_*` 检查、废弃通道拒绝测试 | 合法覆盖；本轮不改测试 |
| 需要修复 / 需主 Agent 复核 | `backend/app/services/account_bot_runtime.py`、`backend/app/worker/runtime.py`、`backend/app/worker/plugins/*`、`frontend/src/pages/Accounts/BotTab.tsx`、`docs/examples/plugin_context_minimal.py` 仍有旧 hook、旧 notice 或旧平铺 payload 命中 | 超出本轮允许写入范围或属于 runtime/UI；需主 Agent 按最终版 blocker 逐项确认是否已迁移或仍需修复 |

聚合统计：

| 审计 | 主要命中 |
| --- | --- |
| 旧机制审计 | 文档/README/示例/脚本命中均已归类；后端 runtime、官方/内置插件、前端 BotTab 和大量测试仍命中，需要最终集成复核 |
| 插件 payload 主路径审计 | 文档命中均为“不要使用旧平铺字段”的迁移说明；后端 `event_trace` / `events` / runtime / 官方插件仍有兼容读取，需 runtime Owner 判断 |
| 新契约覆盖审计 | 后端 schema/service/test、前端类型/UI、文档、示例和验证脚本均有命中；覆盖存在但未代表全部场景已人工验收 |
| 发送通道审计 | `interaction_bot` / `userbot_reply` / `auto` / `notice` 命中覆盖计划、文档、runtime、前端和测试；旧 `notice` 是否完全不可执行需 runtime/UI 复核 |
| Trace 覆盖审计 | `event_trace.py`、`account_bot_runtime.py`、`worker/command.py`、`interaction/delivery.py`、日志 API 和测试均有 `trace_id` / `record_span` / `record_action` 命中 |

### 新契约覆盖审计

命令：

```bash
rg -n "event_subscriptions|capabilities|telegram_native_raw|native_raw_meta|answer_inline_query|chosen_inline_result|settlement|send_channel_deprecated" README.md docs backend frontend examples scripts
```

状态：已执行；命中覆盖 README、docs、backend schema/service/test、frontend 类型/UI、examples、scripts。覆盖存在，但 runtime、UI 和人工验收仍待最终复核。

### 插件 payload 主路径审计

命令：

```bash
rg -n "payload\\[\"text\"\\]|payload\\.get\\(\"text\"\\)|payload\\[\"chat_id\"\\]|payload\\.get\\(\"chat_id\"\\)" docs backend examples
```

状态：已执行；文档命中均是废弃/迁移提示。后端兼容读取命中包括 `event_trace.py`、`events.py`、`account_bot_runtime.py`、`worker/runtime.py`、官方/内置 `game24` / `math10`，本轮不改 runtime。

### 发送通道审计

命令：

```bash
rg -n "send_via|channel_selector|interaction_bot|userbot_reply|auto|notice" backend frontend docs examples
```

状态：已执行；命中量大，包含计划/迁移说明、前端状态展示、后端 delivery/contracts、官方/内置插件、测试。旧 `notice` 可执行性需要主 Agent 用 runtime/UI 验收确认。

### Trace 覆盖审计

命令：

```bash
rg -n "start_trace|record_span|record_action|finish_trace|trace_id|reason_code" backend/app
```

状态：已执行；命中集中在 `event_trace.py`、`account_bot_runtime.py`、`worker/command.py`、`interaction/delivery.py`、日志 API 和测试。仅说明代码面存在 Trace 接入点，不等于五条真实链路已经通过。

## 本轮验证记录

以下命令记录是历史验证基线。若当前工作区在这些命令之后又修改了 runtime、前端、文档验证脚本或版本文件，最终报告必须重新执行并覆盖本表，不能直接复用这些结果。

| 命令 | 状态 | 记录 |
| --- | --- | --- |
| `cd backend && .venv/bin/ruff check app` | 历史通过 | `All checks passed!` |
| `cd backend && .venv/bin/pytest -q` | 历史通过 | `1013 passed, 2 skipped, 7 warnings in 36.73s` |
| `cd backend && .venv/bin/alembic heads` | 历史通过 | `0031 (head)` |
| `cd backend && .venv/bin/alembic upgrade head --sql >/tmp/telepilot-alembic-0.40.0.sql` | 历史通过 | 生成 873 行 SQL；在线 `alembic upgrade head` 因本机 PostgreSQL 5432 未启动且 Docker daemon 不可用未验证 |
| `backend/.venv/bin/python scripts/validate-plugin-examples.py` | 历史通过 | `ok: event_bus_demo`、`ok: with_ai`、`ok: with_http`、`ok: with_interaction`；`translate` 按历史示例跳过 |
| `backend/.venv/bin/python scripts/validate-installed-interaction-plugins.py` | 历史通过但有 warning | `dice_grid_hunt`、`guess_number`、`lottery_plus`、`poetry_blank`、`pt_promote`、`redpack-byRBQ` 均缺 usage 或仍以旧 `interaction_entries` 为主声明；脚本退出 0，表示可安装态检查完成，不表示已迁移完成 |
| `cd frontend && ./node_modules/.bin/tsc -b --pretty false` | 历史通过 | 与生产构建串行执行成功 |
| `cd frontend && ./node_modules/.bin/vite build` | 历史通过 | `✓ built in 3.62s`，PWA 生成 `dist/sw.js` 和 `dist/workbox-9c9670a4.js` |
| `git diff --check` | 历史通过 | 无输出，退出 0 |
